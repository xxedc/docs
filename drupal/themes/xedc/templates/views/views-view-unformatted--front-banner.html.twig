{#
  views-view-unformatted--front-banner.html.twig
  精选内容 — 侧边栏紧凑列表
#}
<div class="xedc-featured-list">
  {% for row in rows %}
    <div class="xedc-featured-list__item">{{ row.content }}</div>
  {% endfor %}
</div>
