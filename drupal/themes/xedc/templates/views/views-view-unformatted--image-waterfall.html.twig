{#
  views-view-unformatted--image-waterfall.html.twig — 瀑布流卡片网格
  外层结构（工具栏/分页）由 views-view--image-waterfall.html.twig 控制
#}
<div class="xedc-masonry" id="xedc-masonry" data-cols="3">
  {% for row in rows %}
    <div class="xedc-masonry__item" data-index="{{ loop.index }}">
      {{ row.content }}
    </div>
  {% endfor %}
</div>
