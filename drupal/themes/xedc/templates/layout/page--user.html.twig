{#
  page--user.html.twig — 用户页面（个人中心 / 编辑资料）
  全宽无侧边栏布局
#}
<!DOCTYPE html>
<html{{ html_attributes }}>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ head_title|safe_join(' | ') }}</title>
  {{ head }}
  {{ styles }}
  {{ scripts_top }}
</head>
<body{{ attributes.addClass('xedc-body', 'xedc-body--user') }}>
{{ page_top }}

{# ── 顶部导航栏 ── #}
<header class="xedc-navbar" role="banner">
  <div class="xedc-container xedc-flex xedc-flex-between" style="height:56px;">
    <a href="{{ url('<front>') }}" class="xedc-navbar__logo" aria-label="{{ site_name }}">
      <span class="xedc-navbar__logo-text">{{ site_name }}</span>
    </a>
    <nav class="xedc-navbar__nav" role="navigation" aria-label="主导航">
      {{ drupal_menu('main') }}
    </nav>
    <div class="xedc-navbar__search">
      <form action="/search" method="get" role="search">
        <input type="search" name="q" placeholder="搜索内容..." class="xedc-search-input" aria-label="全站搜索" />
        <button type="submit" class="xedc-search-btn" aria-label="搜索">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/>
          </svg>
        </button>
      </form>
    </div>
    <div class="xedc-navbar__tools xedc-flex xedc-items-center xedc-gap-2">
      {% if is_logged_in %}
        <a href="/messages" class="xedc-navbar__icon-btn" aria-label="消息通知">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/>
            <path d="M13.73 21a2 2 0 0 1-3.46 0"/>
          </svg>
        </a>
      {% endif %}
      <div class="xedc-theme-toggle" data-xedc-theme-toggle>
        <button class="xedc-navbar__icon-btn" data-xedc-theme-icon aria-label="切换主题" aria-haspopup="true" aria-expanded="false">🌙</button>
        <div class="xedc-theme-dropdown" data-xedc-theme-dropdown role="menu" aria-hidden="true">
          <button class="xedc-theme-dropdown__item" data-xedc-theme-option="system" role="menuitemradio">🖥️ 跟随系统</button>
          <button class="xedc-theme-dropdown__item" data-xedc-theme-option="light" role="menuitemradio">☀️ 日间</button>
          <button class="xedc-theme-dropdown__item" data-xedc-theme-option="dark" role="menuitemradio">🌙 夜间</button>
          <button class="xedc-theme-dropdown__item" data-xedc-theme-option="midnight" role="menuitemradio">⚫ 深夜</button>
        </div>
      </div>
      {% if is_logged_in %}
        {% set _hues = [220, 280, 155, 30, 340, 190, 320, 55] %}
        <a href="/user/{{ current_user.id() }}" class="xedc-navbar__avatar" aria-label="个人中心">
          {% if current_user_picture %}
            <img class="xedc-avatar xedc-avatar--sm xedc-avatar--img"
                 src="{{ current_user_picture }}" alt="{{ current_user_name }}" />
          {% else %}
            <div class="xedc-avatar xedc-avatar--sm"
                 style="background:oklch(0.62 0.16 {{ _hues[(current_user_name|default('U')|length) % 8] }});">
              {{ (current_user_name|default('U'))|slice(0,1)|upper }}
            </div>
          {% endif %}
        </a>
      {% else %}
        <a href="/user/login" class="xedc-btn xedc-btn--default xedc-btn--sm">登录</a>
      {% endif %}
    </div>
  </div>
</header>

{# ── 系统消息 ── #}
{% if page.highlighted %}
  <div class="xedc-highlighted" role="alert">
    <div class="xedc-container">{{ page.highlighted }}</div>
  </div>
{% endif %}

{# ── 主内容（全宽）── #}
<main class="xedc-user-page__main" id="main-content" role="main" tabindex="-1">
  {{ page.content }}
</main>

{# ── Footer ── #}
<footer class="xedc-footer" role="contentinfo">
  <div class="xedc-container">
    <nav class="xedc-footer__nav" aria-label="底部导航">
      {{ drupal_menu('footer') }}
    </nav>
    <div class="xedc-footer__bottom">
      <p class="xedc-text-xs xedc-text-muted">© {{ "now"|date("Y") }} {{ site_name }}. All rights reserved.</p>
    </div>
  </div>
</footer>

{{ page_bottom }}
{{ scripts }}
</body>
</html>
